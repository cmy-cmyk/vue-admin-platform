# 生产环境 API 地址
# - Vercel 部署时:在 Vercel 项目设置 → Environment Variables 配置此变量
#   值为后端 Render 地址,例如 https://vue-admin-platform-server.onrender.com/api
# - 此处保留 /api 仅作占位,实际值由 Vercel 环境变量注入覆盖
# - 本地预览构建(npm run build && npm run serve)走 vite proxy,可用 /api
VITE_API_BASE_URL=/api
